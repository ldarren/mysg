# Start
- git clone https://github.com/ldarren/mysg.git
- npm i
- cp config/build/mysql-1.dev.json config/YOUR_CONFIG
- change the web and redis setting to your environment
- node node_modules/picos/lib/app.js -p config/YOUR_CONFIG

# TODO
- allow 9pm time format
- atomic join
